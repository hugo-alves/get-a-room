# Original build brief

This directory preserves the original Portuguese implementation brief and acceptance notes that led to the first Get A Room prototype.

It is historical context, not the canonical public contract. Current behavior is defined by:

- `openapi.yaml` for HTTP;
- `docs/protocol.md` for room semantics;
- `docs/security.md` for the threat model; and
- the executable tests under `test/`.
